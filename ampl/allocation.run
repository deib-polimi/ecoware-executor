model allocation.mod
data allocation0.dat
option solver cplex;
solve;
display tier_used, use_tier_weight, not_use_tier_weight;
display vm_used, cpu_max, mem_max, use_vm_weight, not_use_vm_weight;
display cpu_demand, mem_demand;

display tier_usage, tier_changed, tier_idle, cpu, mem;
display vm_usage, vm_idle;