Vagrant.configure("2") do |config|
  config.vm.provider "virtualbox" do |v|
    v.memory = 512
    v.cpus = 2
  end
  config.vm.box = "docker-set"
  config.vm.network "forwarded_port", guest: 2376, host: 5000
end

